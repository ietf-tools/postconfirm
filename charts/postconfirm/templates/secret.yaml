{{- if .Values.secret.create -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
type: Opaque
data:
  username: {{ .Values.secret.data.username | b64enc }}
  password: {{ .Values.secret.data.password | b64enc }}
  port: {{ .Values.secret.data.port | b64enc }}
  dbname: {{ .Values.secret.data.postconfirm | b64enc }}
  host: {{ .Values.secret.data.dbserver | b64enc }}

{{- end }}
